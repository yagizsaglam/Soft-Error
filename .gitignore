# Image backups (originals)
webpics-backup/

# macOS
.DS_Store

# Windows
Thumbs.db

# Large source files (not needed for deployment)
*.psd

# Temp scripts
compress-images.py
deployment-dns-report.txt
test-model.html

# Python
__pycache__/

# Claude Code local settings and dev-server config
.claude/

# Generated: the 16MB single-file offline build of chapter 4. The live site
# serves chapter4.html + models/ over HTTP, so this is only for opening the
# sequence locally by double-click. Rebuild it with tools/build_standalone.js
chapter4_standalone.html

# Unused large source files (kept locally only, exceed/approach GitHub limits)
webpics/box.png
webpics/veni.glb
webpics/finalrender1.png

# Portfolio PDF source (257MB — exceeds GitHub 100MB limit; keep locally only)
webpics/Portfolio.pdf

# Backup of the original 2-page spreads (source kept locally only)
webpics/portfolio_spreads/

# Temporary working files (local only)
暂存/

# Claude Code local tooling
.agents/
skills-lock.json
